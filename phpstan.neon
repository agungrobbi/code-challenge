parameters:
    level: 5
    paths:
        - src/
        - tests/
    excludePaths:
        - /vendor/*

    # PHP version compatibility
    phpVersion: 80200

    # Type inference
    inferPrivatePropertyTypeFromConstructor: true

    # Custom extensions
    bootstrapFiles:
        - /vendor/autoload.php
